package gov.dc.surdocs.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebSphereCompatibilityConfig {
    /*
     * Reserved for WebSphere-specific bean overrides if needed.
     * Typical use cases:
     * - JNDI DataSource lookup
     * - container-managed security integration
     * - custom SSL/truststore handling for FileNet WSI
     */
}
